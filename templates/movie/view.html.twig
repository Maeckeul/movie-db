{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Movie View {% endblock %}

{% block body %}

    {{ dump(movie) }}

    {{ dump(movie.category) }}

    {{ dump(movie.category.label) }}

    {{ dump(movie.category) }}

    {{ dump(movie.director) }}

    {{ dump(movie.director.name) }}

    {{ dump(movie.director) }}
    
{% endblock %}
