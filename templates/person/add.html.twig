{% extends 'base.html.twig' %}

{% block title %}

    {{ parent() }} - Ajouter une personne

{% endblock %}

{% block body %}

<h1 class="mt-5">Ajouter une nouvelle personne</h1>

{{ form_start(form) }}

{{ form_widget(form) }}

<a class="btn btn-primary" href="{{ path('homepage') }}">Annuler</a>
<button class="btn btn-primary" type="submit">Ajouter</button>

{{ form_end(form) }}

{% endblock %}